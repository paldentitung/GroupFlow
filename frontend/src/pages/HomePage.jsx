import React from "react";
import Header from "../components/Header";
const HomePage = () => {
  return (
    <div>
      <Header title="GroupFlow" buttonName="Create Group" onClick={() => {}} />
    </div>
  );
};

export default HomePage;
